import React from 'react';
// import 'bootstrap'
// import './Blog.css'

const Blog = (message) => {
  toast.success(`${v}`, {
      position: 'top-right',
      autoClose:false,
    });
  return (
    <div className="container">
      
      
    </div>
  );
};

export default Blog;
